object Modules {

    const val app = ":app"
    const val appAuth = "$app:_auth"
    const val appReddits = "$app:_reddits"

    private const val core = ":core"
    const val coreNetwork = "$core:network"
    const val coreBase = "$core:base"

    private const val data = ":data"
    const val dataCommon = "$data:common"
    const val dataAuth = "$data:auth"
    const val dataStorage = "$data:storage"
    const val dataReddits = "$data:reddits"
}